<!DOCTYPE html>
<html>
<head>
  <title>SupremeMetrics</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <div class="page-header">
      <h1>SupremeMetrics</br><small>Learn what your customers do when they come to see you!</small></h1>
    </div>
  </div>
  <div class="navbar navbar-default">
        <ul class="nav nav-tabs">
          <li><%= link_to "Home", root_path %></li>
          <li><%= link_to "About" %></li>
          <li><%= link_to "Instructions" %></li>
          <div class="container">
        <% if current_user %>
          <li class="pull-right">Logged in as <%= current_user.name %> <b>or</b> <%= link_to "Log Out", logout_path %></li>
        <% else %>
          <li class="pull-right"><%= link_to "Sign Up", signup_path %> or <%= link_to "Login", login_path %></li>
          <% end %>
        </div>
        </ul>
  </div>
  </div>
  <div class="container">
    <%= yield %>
  </div>
    </div>
  </div>
</body>
</html>
